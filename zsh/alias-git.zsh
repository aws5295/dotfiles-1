# rebase
alias irebase='git rebase -i origin/master'
alias grebase='git pull origin master --rebase'

# checks
alias g='git status -sb'
alias gv='git branch -vv'

# branch
ealias gb='git branch'
ealias gco='git checkout'
